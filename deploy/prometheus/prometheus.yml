# prometheus.yml — scrape + evaluación de alertas para el cerebro central de Musubi.
#
# Lo instala deploy/prometheus/install-musubi-prometheus.sh (Prometheus como systemd nativo,
# al estilo de install-musubi-brain.sh). Prometheus corre EN EL MISMO host que el cerebro,
# así que scrapea 127.0.0.1:7717/metrics por loopback (no hace falta abrir el puerto).
#
# El endpoint /metrics EXIGE el mismo bearer token que el resto del cerebro
# (service.auth_token_env). Se pasa por `credentials_file` para que el secreto NUNCA viva en
# este archivo — mismo patrón "token por referencia" que el .mcp.json de los clientes.
#
# ─────────────────────────────────────────────────────────────────────────────────────────────
# PARA VER LAS MÉTRICAS DE LA FLOTA (musubi_fleet_device_*) HACE FALTA UN PASO MÁS
#
# Desde el track «Control de flota», /metrics exporta también la telemetría de cada máquina —
# pero SÓLO las que la credencial del scrape puede ver. Y las capacidades de flota NO se derivan
# del rol: un token admin SIN concesiones explícitas no ve ni una máquina, a propósito (si no,
# el token de Prometheus sería una puerta trasera que sortea el eje de capacidades entero).
#
# Con el token legacy de arriba vas a ver las métricas del SERVIDOR y ninguna de la flota, y la
# propia salida de /metrics te lo dice en un comentario. Para verlas, dale a Prometheus su propio
# principal en .musubi/principals.yaml:
#
#   principals:
#     - name: prometheus
#       token_sha256: "<sha256 del token que pongas en credentials_file>"
#       role: reader
#       read: all                # ve todos los proyectos
#       fleet:
#         metrics: ["*"]         # ...y la telemetría de todas las máquinas
#
# Nada más: NO le des `exec` ni `screen`. Un scraper sólo necesita mirar, y una credencial que
# vive en un archivo de configuración de otro servicio es exactamente la que no querés que
# pueda ejecutar nada.
# ─────────────────────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────────────────────
# SI ENCENDÉS EL EMPUJE OTLP, LAS SERIES DE FLOTA LLEGAN POR DOS CAMINOS Y LAS ALERTAS DISPARAN
# DOBLE. Hay que decidirlo, no descubrirlo.
#
# Desde S11 el cerebro puede EMPUJAR la misma telemetría por OTLP/JSON, además de servirla en
# /metrics (`fleet.otlp.endpoint` en el config del cerebro). Prometheus lo recibe en
# /api/v1/otlp/v1/metrics y SÓLO si corre con `--web.enable-otlp-receiver` (está en el instalador
# systemd y en deploy/docker/compose.yml; sin el flag el POST devuelve 404 y el empuje muere en
# silencio). Los dos caminos emiten las MISMAS series `musubi_fleet_device_*`, y se distinguen
# nada más que por `instance`:
#
#   scrape → instance="musubi-brain"        (la etiqueta del static_config de abajo)
#   push   → instance="musubi-otlp-push"    (service.instance.id del payload, constante del código)
#
# Con los dos activos, cada máquina tiene DOS series por métrica y las 12 reglas de
# rules/musubi-alerts-flota.yml se evalúan dos veces: dos avisos por incidente, y —peor— un
# `count()` o un `sum()` sobre la flota que da el doble.
#
# LA RECETA, si encendés el push y querés seguir scrapeando /metrics (recomendado: el scrape trae
# además las métricas del SERVIDOR, que el push no lleva a propósito): descartá las series de
# flota del scrape y quedate con las del push, agregando al job `musubi` de abajo:
#
#   metric_relabel_configs:
#     - source_labels: [__name__]
#       regex: musubi_fleet_(device|service)_.*
#       action: drop
#
# La otra salida válida es no encender el push: el tirón ya funciona y no necesita nada de esto.
# Lo que NO es una opción es dejar los dos sin decidir. Lo custodia
# TestElReceptorOTLPSeHabilitaEnLosDosLugaresYLaDuplicacionEstaDeclarada.
# ─────────────────────────────────────────────────────────────────────────────────────────────

global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    host: musubi-brain

# UN GLOB, NO UN ARCHIVO. Las reglas están repartidas en TRES archivos porque cada uno se instala
# bajo su propia condición (las de flota sólo si el cerebro expone `musubi_fleet_*`; la del backup
# off-host sólo si hay `BACKUP_REMOTE`). Con una ruta fija, `preparar.sh` copiaba el archivo de
# flota y Prometheus lo IGNORABA — con las 12 reglas en disco y ninguna evaluándose, que es peor
# que no haberlas instalado: se ven puestas.
rule_files:
  - /etc/prometheus/rules/*.yml

# LOS SCRAPES DE SITIO VAN POR GLOB, POR EL MISMO MOTIVO QUE LAS REGLAS. Un `scrape_configs`
# fijo acá obligaría a que la referencia real de una base gestionada —que es medio secreta y
# distinta en cada sitio— viva en el archivo que se versiona. Con el glob, el repo trae el
# `.ejemplo` y cada sitio instala el suyo en /etc/prometheus/scrapes/.
#
# Y si el directorio está vacío no pasa nada: Prometheus arranca con los scrapes de abajo.
scrape_config_files:
  - /etc/prometheus/scrapes/*.yml

# LAS ALERTAS SALEN DE ACÁ HACIA ALGUIEN (S10 · A4). Hasta este track, este bloque era un
# comentario: las reglas se evaluaban y se veían en http://127.0.0.1:9099/alerts, y no notificaban
# a ningún lado. La configuración del destino está en alertmanager.yml, al lado de este archivo.
#
# Si todavía NO montaste Alertmanager, comentá este bloque: Prometheus arranca igual —los envíos
# fallan y quedan en su log— pero conviene que la ausencia sea una decisión y no un error de fondo.
alerting:
  alertmanagers:
    - static_configs:
        - targets: ["127.0.0.1:9093"]

scrape_configs:
  # El cerebro Musubi. El job DEBE llamarse "musubi": la alerta MusubiDown mira up{job="musubi"}.
  - job_name: musubi
    scheme: http
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials_file: /etc/prometheus/musubi.token
    static_configs:
      - targets: ["127.0.0.1:7717"]
        labels:
          instance: musubi-brain

    # UN SOLO PRODUCTOR POR DATO — la mitad que hace que el empuje no sea duplicación.
    #
    # `/metrics` sirve DOS cosas: las métricas propias del cerebro (tools, outbox, cuotas, el
    # estado del propio empuje) y la telemetría de la flota. Cuando `fleet.otlp` está encendido,
    # esa segunda mitad llega ADEMÁS por el empuje, con `instance: musubi-otlp-push`.
    #
    # No se pisan —el label `instance` las distingue— y por eso el daño no se ve como un error:
    # se ve como que todo anda. Lo que pasa es que CADA REGLA DE FLOTA MATCHEA DOS SERIES y cada
    # alerta sale DUPLICADA. Medido acá el 2026-08-28, al encender el empuje: 5 alertas, 10 avisos.
    #
    # Así que el scrape deja de traer flota y se queda con lo suyo. Cada dato tiene un productor.
    #
    # EL COSTO, dicho: si el empuje muere, la flota deja de tener series y las 12 reglas de flota
    # se quedan sin datos con los que dispararse — no fallan, enmudecen. Eso NO queda descubierto:
    # lo cazan `MusubiPushOTLPFallando`, `MusubiPushOTLPMudo` y `MusubiPushOTLPNuncaLlego`, que
    # miran los contadores del empujador. Si alguna vez sacás este bloque, sacá también el empuje;
    # y si sacás el empuje, sacá este bloque. Van juntos, y hay una prueba que lo custodia.
    # SE NOMBRAN LAS DOS FAMILIAS EMPUJADAS, no se barre con `musubi_fleet_.*`. La regla ancha
    # tiraba también `musubi_fleet_policy_actions_total`, que es la ÚNICA de esa familia con UN
    # SOLO PRODUCTOR: sale del scrape y el empuje no la lleva. Con la regla ancha esa serie no
    # existía en Prometheus, y las dos alertas que la miran —`PoliticaQueNoCura` y
    # `PoliticaSinPermiso`— no podían dispararse NUNCA, ni con políticas configuradas.
    #
    # Medido el 2026-08-31: se configuró la primera política real, disparó (`acciones=1` en el
    # barrido, con su línea «política dispara» en el journal), el cerebro contó la acción... y
    # la serie seguía sin existir. La razón anotada hasta entonces —«no hay ninguna política
    # configurada»— era cierta y tapaba ésta. Dos causas, y la de arriba escondía a la de abajo.
    metric_relabel_configs:
      - source_labels: [__name__]
        regex: "musubi_fleet_(device|service)_.*"
        action: drop

  # QUIÉN VIGILA AL VIGILANTE. Sin este job, que la CADENA DE ALERTAS deje de entregar es
  # invisible: Alertmanager lo escribe en su log de contenedor y nadie lee un log de contenedor.
  #
  # No es hipotético. Medido el 2026-08-28: el receptor del dead-man's switch llevaba **32 horas
  # fallando cada 5 minutos** —387 errores, `url_file` a un archivo que no existe— con el mismo
  # Alertmanager entregando perfecto por Telegram. 310 intentos por webhook, 279 fallos; 31 por
  # Telegram, 0 fallos. Un canal roto al lado de uno sano, y nada lo contaba.
  #
  # LO QUE ESTE JOB NO PUEDE HACER, dicho: si la cadena entera muere, la alerta que avisa de eso
  # tampoco sale. Eso no lo arregla ningún scrape — lo arregla el dead-man's switch EXTERNO, que
  # es justamente lo que estaba roto. Este job cubre el caso real y frecuente: UN receptor caído
  # mientras otro anda.
  - job_name: alertmanager
    static_configs:
      - targets: ["127.0.0.1:9093"]
    metric_relabel_configs:
      # 472 series expone, y 410 son `alertmanager_*`. El resto es el runtime de Go, que para
      # esto no dice nada.
      - source_labels: [__name__]
        regex: "up|alertmanager_.*"
        action: keep

  # Prometheus se scrapea a sí mismo: permite distinguir "el cerebro cayó" de "Prometheus cayó".
  #
  # EL 9099 NO ES UN CAPRICHO. 9090 es el puerto por defecto de **Cockpit**, que viene instalado
  # en muchos servidores Linux y lo ocupa. Los dos fallos que evita: Prometheus que no arranca por
  # «address already in use», y —peor— alguien que apunta el navegador al 9090, ve una UI, y cree
  # que Prometheus está andando cuando lo que contesta es Cockpit.
  #
  # Este target tiene que coincidir con `PROM_ADDR` del instalador systemd y con
  # `--web.listen-address` del compose. Lo custodia TestElPuertoDePrometheusEsElMismoEnTodosLados.
  - job_name: prometheus
    static_configs:
      - targets: ["127.0.0.1:9099"]
