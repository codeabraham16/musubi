{
  "name": "musubi-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build del brain-dashboard WebGL (three.js) embebido en el binario Go via go:embed. El bundle (dashboard.bundle.js) se COMMITEA; compilar Go NO necesita node. Solo se corre 'npm run build' al cambiar el frontend.",
  "scripts": {
    "build": "esbuild src/dashboard.mjs --bundle --minify --format=iife --target=es2020 --legal-comments=none --outfile=dashboard.bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "three": "0.169.0"
  }
}
